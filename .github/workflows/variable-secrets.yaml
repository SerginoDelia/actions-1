name: Exploring Variables and Secrets

on: 
    push

jobs:
    # docker:
    #     runs-on: ubuntu-latest
    #     steps:
    #     - name: Docker Build
    #       run: docker build -t docker.io/dockerUsername/imageName:latest

    #     - name: Docker Login
    #       run: docker login --username=dockerUsername --password=SecuredPassword

    #     - name: Docker Publish
    #       run: docker push docker.io/dockerUsername/imageName:latest
    
    # deploy:
    #     needs: docker
    #     runs-on: ubuntu-latest
    #     steps:
    #     - name: Docker Run
    #       run: docker run -d -p docker.io/dockerUsername/imageName:latest

############ environment variables at STEP level ####################

    # docker: 
    #     runs-on: ubuntu-latest
    #     steps:
    #     - name: Docker Build
    #       env: 
    #         CONTAINER_REGISTRY: docker.io
    #         DOCKER_USERNAME: serge716
    #         IMAGE_NAME: github-actions-nginx
    #       run: echo docker build -t $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

    #     - name: Docker Login
    #       env: 
    #         DOCKER_PASSWORD: SecuredPassword
    #         DOCKER_USERNAME: serge716
    #       run: echo docker loging --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD

    #     - name: Docker Publish
    #       env: 
    #         CONTAINER_REGISTRY: docker.io
    #         DOCKER_USERNAME: serge716
    #         IMAGE_NAME: github-actions-nginx
    #       # can also use ${{ env.ENV_VARIABLE_NAME }}
    #       run: echo docker push ${{ env.CONTAINER_REGISTRY }}/$DOCKER_USERNAME/$IMAGE_NAME:latest

    # deploy:
    #     needs: docker
    #     runs-on: ubuntu-latest
    #     steps:
    #     - name: Docker Run
    #       env: 
    #         CONTAINER_REGISTRY: docker.io
    #         DOCKER_USERNAME: serge716
    #         IMAGE_NAME: github-actions-nginx
    #       run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

############# environment variables at the JOB level #################

    docker:
      env:
        CONTAINER_REGISTRY: docker.io
        DOCKER_USERNAME: serge716
        IMAGE_NAME: github-actions-nginx
      runs-on: ubuntu-latest
      steps:
      - name: Docker Build
        run: echo docker build -t $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest

      - name: Docker Login
        env: 
          DOCKER_PASSWORD: securedPassword
        run: echo docker login --username=$DOCKER_USERNAME --password=$DOCKER_PASSWORD

    deploy:
        env:
          CONTAINER_REGISTRY: docker.io
          DOCKER_USERNAME: serge716
          IMAGE_NAME: github-actions-nginx
        needs: docker
        runs-on: ubuntu-latest
        steps:
        - name: Docker Run
          run: echo docker run -d -p 8080:80 $CONTAINER_REGISTRY/$DOCKER_USERNAME/$IMAGE_NAME:latest
